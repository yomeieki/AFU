# 阿福凉菜 上线进度总表

> **更新时间**：2026-09-07 00:40
> **生产版本**：`806c2a3`（`apps/` 代码已是最新，之后的提交全是文档与测试，不需要部署）
>
> **这份表的用途**：`docs/miniapp-release-checklist.md` 那份提审清单里 41 项未打勾，
> 但**其中一大半早就做完了、只是没人回来打勾**——照着念会去重做已完成的事。
> 这份表是**逐项实测过**的当前真相，每条带判据。
>
> 图例：`[x]` 已完成且已验证 · `[ ]` 未完成 · 🔴 阻塞上线 · 🟡 上线前该做 · ⚪ 不阻塞

---

## 一、本轮（2026-09-06 → 09-07）完成的

### 同城配送：从零到跑通真单

- [x] **同城总开关开启** —— `/api/local/meta` 的 `data.enabled = true`
- [x] 建 `LOCAL` 分类「凉菜」+ 2 件真商品（凉拌牛肉 ¥25 / 凉拌三丝 ¥15，库存 10）
- [x] 生产 nginx 补上 `location /api/kd/`（含显式 `gzip off`）
      —— 实测：`/api/kd/` 的响应无 `content-encoding`，对照组 `/api/products` 有 gzip
- [x] 配送通知订阅消息模板配入生产并重启生效 —— 生效模板数 3（发货/退款/配送），未超微信 3 个上限
- [x] **真机跑通全链路**：下单 → 付款 → 接单 → 备菜 → 呼叫 → 匹配骑手 → 取货 → 买家收货
      —— 订单 `ORD20260906918208`，全程 32 分钟，6 条回调一条没丢

### 本轮修掉的 4 个 bug（3 个是「从上线起就坏、没人发现」的静默失败）

- [x] **`price()` 的 `callbackUrl` 写成空串** → 生产每次查价都返 `30001`，静默退回「直线×1.7」估算，
      顾客看到的配送费从来不是真实道路距离算的（`26677a2`）
- [x] **`queryCourier` 传 `taskId` 而接口要 `orderId`** → 骑手位置从上线起一次都没成功过，
      失败被 catch 吞成 `location:null`（`806c2a3`）
- [x] **支付回调无守卫写会覆盖并发取消** → 已释放的券与积分会留在一张 PAID 单上（`b6fd384`）
- [x] **小程序两个死按钮**：「导入微信收货地址」缺 `chooseAddress` 声明、售后「添加照片」失败全静默（`34c1446`）

### 本轮查清的经济性事实（改了对同城的判断）

- [x] **实扣 = 中标运力报价**（假设成立）—— 快递100 后台四笔预扣与库里报价快照**四比四全中**
- [x] **并呼让最贵的抢到**：达达报 ¥16.23、闪送 ¥23.32 中标，**每单多付 ¥7.09**
- [x] **并呼是余额放大器**：一单冻结 ¥75.08 实付 ¥23.32；100 元余额**只能同时挂 1 单**
- [x] 修正「远单必亏」这个错框架 —— 短单最坏亏 ¥5.22 > 远单 ¥2.32，**根因是并呼不是距离**
- [x] 并呼未中标方**不推 720**（首单 7 家并呼、6 家落空、零条 720）—— 全链路最危险的未知，样本 n=1

### 会员

- [x] **同城单用券回归**（同城开通后才验得了的那一项）—— 新增 e2e 第 49 段，六组全绿
- [x] 核实「券完全独立于积分开关」—— `points.enabled` 只管发分，`coupons.ts` 全文不读它
- [x] 核实积分/券/赠品/新客券**四项都能在后台自己调** → 「交给店员」的策略不需要开发
- [x] 店员手册第七章补「定价：积分换多少、券发多少」一节（`08013b0`）

### 测试基础设施

- [x] **修掉一个会让整个 e2e 静默截断的写法** —— 后台 60 秒心跳撞上手工 tick 时
      `runSchedulerTick` 返回空对象而 HTTP 仍是 `code 0`，`null` 进算术上下文把脚本掐死在 §30，
      **退出码还是 0**，所以 §31 往后连同整个 `e2e.d`（8 个文件）已经沉默地没在跑。
      修前跑到 30 段，修后跑完 50 段
- [x] 历史遗留 14 笔联调单打上 `isTest`，生产统计基线归零

### 文档

- [x] `docs/local-delivery-golive-manual.md` —— 店主照着点的同城开通七步
- [x] `docs/local-delivery-run-log.md` —— 首单全部实测原始数据
- [x] `docs/superpowers/plans/2026-09-06-local-delivery-remediation.md` —— 同城整改方案（五批次）
- [x] `docs/superpowers/notes/2026-09-06-local-delivery-handoff.md` —— 交接说明（十节踩坑清单）
- [x] 修掉三处文档与代码不符：店员手册两个不存在的按钮、`deployment.md` 里
      「透传原始字节验签」这个错理由、调研文档里悬了三天的「预扣规则待与客服确认」

---

## 二、早前已完成（清单里没打勾，本轮实测确认）

**这一组不用再做。清单该回勾。**

- [x] 订阅消息模板填入生产 `.env` —— 发货 ✔ 退款 ✔ 配送 ✔（付款**故意留空**，配了就 4 个会把配送挤掉）
- [x] `isDev = false`、`baseURL` 指向生产
- [x] `project.config.json` 的 appid = `wx11aa80fcd1bfd2bb`，与公众平台一致
- [x] `config/shop.js` 无「【待填】」残留 —— 实测 0 处
- [x] `scope.userLocation.desc` ≤ 30 字 —— 实测 29 字
- [x] 封面无「即将开通」占位、无 TODO —— 实测各 0 处
- [x] 生产 `.env`：五项 mock 全 `false`、`NODE_ENV=production`、`COS_*` 已配
- [x] COS 图片直传通道 —— 已有一张图落在 COS 域名并公网 200（`uploads/` 原本就是空的，无存量需迁移）
- [x] 一分钱实测 / 部分退款 / 售后传图 —— M5 联调与本轮同城真单都跑过
- [x] 会员规则在小程序内公示、用户协议含「六、会员积分与优惠券」
- [x] 封面三个会员热区进真页面
- [x] 每日备份 cron 已生效（`0 2 * * *` → 私有桶 `food-shop/` 前缀，已实跑成功）

---

## 三、未完成 🔴 阻塞上线 —— 卡在店主

- [ ] 🔴 **邮寄渠道一件真商品都没有**
      实测：邮寄 8 件**全部下架**且是开发种子数据（`seed.ts` 里能对上名字），只有 1 件有图。
      同城那 2 件真商品**也没图**（顾客端显示灰色占位块）。
      → 这是「店里没货」的另一半。之前的计划把它推到下一轮，不是漏掉。
- [ ] 🔴 **业务域名校验卡在半路**
      校验文件 `6OLbtqMNyk.txt` 已放好并能公网访问，**但需要你回公众平台点一次「保存」才算完成**。
      「我的 → 商家管理 → 进入管理后台」这个功能依赖它生效。
- [ ] 🔴 **微信认证 + 提审 + 发布**
      清单第一、二节。要等审核，越早开始越好。
- [ ] 🟡 商品图（同城 2 件 + 邮寄铺货时一起）

---

## 四、未完成 🟡 已交接给另一个窗口

同城整改方案五个批次，全文 `docs/superpowers/plans/2026-09-06-local-delivery-remediation.md`，
前置与踩坑在 `docs/superpowers/notes/2026-09-06-local-delivery-handoff.md`。

- [ ] 🟡 **批次 0（零代码，今天就能做）**：骑行均速 15 → 22、运费表按账调、满额免运费门槛
- [ ] 🔴 **批次 1**：只呼最低价 + 3 分钟无人接再并呼 + `actualFee` 自动补齐
      —— **每单省 ¥7.09，且把余额占用从 4 倍降到 1 倍**。这是全项目里唯一在持续流血的地方
- [ ] 🟡 **批次 2**：后台显示中标运力/各家报价/实扣、骑手实时位置接入管理端、
      **后台时间统一按东八区**（现在 24 处日期渲染跟着看的人的电脑走）
- [ ] ⚪ **批次 3**：顾客端订单入口（同城页是个孤岛，下单后回不去菜单也找不到订单）
- [ ] ⚪ **批次 4**：生产 MySQL 时区钉 UTC + 文档

---

## 五、未完成 —— 会员，等你的决定

- [ ] 🟡 **建第一张真券并上架**（你已定：满 ¥50 减 ¥5，全渠道）
      → 后台「优惠券」页新建，字段与点法见本次会话给的表。
      ⚠️ **「发放总量」是唯一的花钱闸门**，留空 = 不限量；建议先填 100（敞口 ¥500）。
      ⚠️ **点「启用」那一刻就对全体真实顾客可见可领，没有第二道开关。**
- [ ] ⚪ 清掉 3 个【内部联调】¥0.01 券模板（现全部已停用，留着无害）
- [ ] 🔴 **开积分开关之前的必做项**：生产有 10 条 `points_ledgers` 是联调残留。
      开关一开那些分就是真能花的钱，而 6 个用户里**混着你自己的真实微信号**（同城首单就是它下的），
      不能一把 `DELETE`。要按 `ref_id` 带 `m2e2e-` 前缀区分「联调造的」与「真实消费得的」，只清前者。
- [ ] ⚪ 积分赠品保持 0 —— **现在填也兑不了**（开关关着，顾客余额恒为 0，全是置灰按钮），
      空态比「看得见摸不着」诚实。等要开开关之前再填
- [ ] ⚪ 新客券保持不发 —— `templateId = null` 时代码第一行就 return，**不发、不报错、零风险**。
      上线后你或店员在后台两步设（先建 `NEWCOMER` 模板上架、再到「会员设置」选中），后台有三态健康提示
- [ ] 🔴 **打开积分开关** —— 全部设计好之后你亲自开。**不可逆：发出去的分收不回**

---

## 六、未完成 🟡 运维

- [ ] 🟡 **UptimeRobot 监控一直没建** —— 步骤在 `deployment.md` 第十三节。
      服务挂了没有任何外部探活会告诉你
- [ ] 🟡 **提审清单本身要回勾一轮** —— 41 项未打勾里一大半已完成（见本表第二节）。
      不修的话你正要开始的提审会被它误导
- [ ] 🟡 **我今天引入的一处文档漂移**：清单 2.4b「隐私与位置能力一致性」表还写着
      `requiredPrivateInfos = ["chooseLocation"]`，而代码已是两项（加了 `chooseAddress`）；
      新增的「相册/照片」这条隐私路径也要补进那张表。
      那一节结尾自己写着「任何改动位置接口的 PR 都要重新走一遍这张表」——我没走完

---

## 七、技术债 ⚪ 不阻塞上线

- [ ] ⚪ **无 CI、无 lint 配置** —— 现在靠人工跑 `tsc` + e2e。
      （原 backlog 里的「零测试」已经不成立：e2e 现有 996 条断言、50 段）
- [ ] ⚪ `orders.ts` 臃肿、service 层未抽取
- [ ] ⚪ `Delivery.actualFee` 全仓从不写入（整改批次 1 会补上）
- [ ] ⚪ 快递100 **没有查余额接口**（实打 11 个方法名全灭），余额预警只能靠「熔断已发生」这个事后信号

---

## 附：一句话总结

**技术侧基本没有欠账了**——支付、退款、打印、备份、监控告警、同城全链路、会员全链路都通过真机验证。

**现在真正拦着上线的是三件不属于代码的事**：
1. **店里只有一半的货**（邮寄零真商品、同城无图）
2. **微信认证与提审还没开始**（要等审核）
3. **同城每单在流血 ¥7.09**（方案已交接，批次 1 是唯一在持续花钱的问题）
