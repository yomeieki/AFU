# Codex Task 7 — 订单详情同城分支

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 7（约 L802）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 7，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken（本 Task 读订单已落库字段，不再报价）。服务端零改动。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

目标：订单详情 LOCAL 分支——顾客文案表、本地时间线、骑手卡、地图/30s 轮询、取消窗口、隐藏快递卡。地图走 Task 0 结论（<map> 或 cover-view 静态）；轮询逻辑两案相同。

### 文件清单
- Modify: apps/miniapp/pages/order/detail.{js,wxml,wxss,json}

### 接口
- Consumes：GET /orders/:id（delivery 白名单、canRequestCancel、cancelRequestDeadline、cancelRequestedAt、distanceM、estimatedDeliveryAt、receiverPoiName…）、GET /orders/:id/courier、POST /orders/:id/cancel-request、GET /local/meta（门店坐标，缓存一次）
- 不改服务端

### 步骤要点
1. DELIVERY_CUSTOMER_LABEL：正常态中文；REASSIGNING/ABNORMAL/UNKNOWN/FAILED/CANCELLED 一律「配送正在协调中」+ 副文案「如超过预计时间请联系商家」。勿直接渲染服务端异常 statusLabel。
2. buildLocalTimeline（勿改邮寄支）：提交→支付→接单备餐→骑手接单→到店→配送中→已送达。done 判据按计划；取消/退款复用邮寄逻辑，仅「已发货」改「配送中」。
3. 骑手卡：ACCEPTED/ARRIVING/ARRIVED/DELIVERING 显示姓名+公司+联系骑手（makePhoneCall，空则隐藏按钮）。
4. startCourierPoll 30s：仅 LOCAL + 在途态；location null 不渲染空图不 toast；onShow 启、onHide/onUnload 停、终态不启。挂在既有 startTicker 同位置，勿第二套生命周期。markers：门店/收货/骑手；中心优先骑手。
5. 取消窗口：信任服务端 canRequestCancel/deadline，勿自算 acceptedAt+5min。三态：已申请灰条；可申请按钮+grace 文案+modal（全额退含配送费）→requestCancelOrder→刷新；否则「已开始制作…」。42229 toast 原样+刷新。PAID 未接单仍走既有 canSelfCancel。
6. LOCAL：快递卡 wx:if=!isLocal；换同城配送信息卡（poi+地址·距门店·预计·配送费）。
7. node --check detail.js。手工：PAID 秒退；接单后申请取消；grace 过期；mock 100/210/230/310/520；515 中性无「改派」；onHide/onShow 轮询；终态无 /courier；邮寄单快递卡/时间线逐字不变。

### 测试断言
- 上述 7 条手工全过；终态 Network 无 /courier（最易漏）
- 邮寄详情零同城元素

### Done
- [ ] LOCAL 分支完整；邮寄零回归；轮询生命周期正确
- [ ] 中文 commit：feat(miniapp): 订单详情同城分支（时间线/骑手/地图轮询/取消窗口）

### 明确不做
- 不改服务端；不暴露改派/异常运营词；不改 confirm.js
- 不 push/密钥/生产/:3100

完成后用简短中文回报：七条验收勾选 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 7 订单详情同城分支 |
| 文件清单 | pages/order/detail.{js,wxml,wxss,json} |
| 接口契约 | /orders/:id；/courier；cancel-request；/local/meta |
| 测试断言 | 七条手工；终态停轮询；邮寄零回归 |
| Done | 见上勾选 |
| 明确不做 | 服务端改动 / 暴露运营异常词 |
| 模型 | Terra |
