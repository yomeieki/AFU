# Codex Task 3 — 购物车隔离的两处收口

**模型档：Terra**
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 3（约 L414）
**拍板 D-007：** D-007 已拍板：①首页临时入口方案 A（同城/邮寄双卡片）②地址列表只显「直线约 x.x km」不调道路距离③同城不做立即购买，只加购→结算。（本任务落实③：LOCAL 商品详情隐藏立即购买）

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 3，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100。

工作目录：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

硬约束（全部写死，不得违反）：
- 工作目录必须 cd 到活线后再动手（见下）。
- 后端 :3100 / 库 food_shop_sc 常驻：勿另起、勿杀。
- 不 push、不碰密钥、不改生产、不做真机真钱。
- 邮寄路径零回归；不改 pages/order/confirm.js 的 deliveryType: 'EXPRESS' 写死。
- 禁止扩 scope：不做封面页、不做 KD100/飞鹅、不动 apps/admin、不加固 M1/M2、不做预约/搜索/会员。
- ES5 风格（var/function/self=this）；金额/距离一律来自服务端（Task 4 直线粗估除外）。
- 提交用中文 type(scope): 摘要 + Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>。

目标：邮寄购物车空态提示「同城还有 N 件」+ 商品详情按 product.channel 分叉（LOCAL 隐藏立即购买）。

### 文件清单
- Modify: apps/miniapp/pages/cart/index.js、index.wxml
- Modify: apps/miniapp/pages/product/detail.js、detail.wxml

### 接口契约
- Consumes: getCart('LOCAL')（仅邮寄车空态时额外查一次）
- Produces: 跨渠道提示点击 → navigateTo('/pages/local/index')

### 步骤要点
1. cart/index.js loadCart：items.length===0 时才 getCart('LOCAL') 算 localPendingCount；有货则置 0。wxml 空态加可点 tip「你在同城配送还有 N 件未结算」→ goLocal。
2. product/detail：按 product.channel —— LOCAL：标签「同城配送」；隐藏「立即购买」（D-007③）；「加入购物车」→「加入同城购物车」；toast + 第二按钮「去同城结算」→ local/index；不调 updateCartCount。EXPRESS：一行不变。
3. 前端隔离不是安全边界；服务端 42224 仍在——勿放松。

### 测试断言
- node --check cart/index.js + product/detail.js 零输出
- 同城加购后进 tabBar 购物车 → 空态 + 跨渠道提示；点进同城菜单
- 邮寄商品详情两按钮与文案与改动前逐字相同

### Done 勾选
- [ ] 空态跨渠道提示只在空态查 LOCAL
- [ ] LOCAL 详情无立即购买；EXPRESS 零回归
- [ ] 中文 commit（计划 Step 4）

### 明确不做
- 不改购物车勾选/结算主流程；不做同城 tabBar 车
- 不改 order/confirm EXPRESS；不做封面/飞鹅

完成后回报：两处改动摘要 + 邮寄详情是否零回归 + 文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 3 购物车隔离收口 |
| 文件清单 | pages/cart/index.js/wxml；pages/product/detail.js/wxml |
| 接口契约 | getCart('LOCAL')（空态）；navigate local/index |
| 测试断言 | 空态提示；LOCAL 无立即购买；EXPRESS 文案不变 |
| Done | 见上勾选 |
| 明确不做 | 主结算流程 / EXPRESS confirm / 封面 |
| 模型 | Terra |
