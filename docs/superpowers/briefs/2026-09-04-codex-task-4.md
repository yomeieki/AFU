# Codex Task 4 — 地址列表的同城选择模式

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 4（约 L470）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 4，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken。
地址列表只显直线粗估（D-007），禁止逐地址调 /local/quote。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

目标：地址列表 LOCAL 选择模式（可配送状态标签 + 补充定位入口）。不动 pages/address/edit（M1 已落地）。

### 文件清单
- Modify: apps/miniapp/pages/address/list.{js,wxml,wxss}

### 接口
- Consumes：GET /addresses（含 latE6/lngE6/poiName）、GET /local/meta（store 坐标 + radiusStraightKm）
- Produces：选中写 app.globalData.selectedAddress 后 navigateBack（既有范式）
- 禁止：列表逐行 POST /local/quote

### 步骤要点
1. onLoad：mode/channel（channel===LOCAL 才 LOCAL，否则 EXPRESS）。onAdd/onEdit 在 LOCAL 透传 ?channel=LOCAL 给 edit。
2. LOCAL 每行三态标签：无坐标→灰「需补充定位」点击进 edit?id=&channel=LOCAL（不选中）；直线≤radiusStraightKm→「直线约 x.x km」；>→「直线约 x.x km · 可能超范围」（橙色，不禁用）。haversine 仅用 meta.store 与地址坐标，私有 12 行函数不导出。注释必须写清：列表 km 非计费距离；不逐行 quote（外呼+30/min 限流）；真正 42220 由确认页服务端结论判。
3. EXPRESS：不拉 meta、不渲染标签、不改点击；新 UI 用 wx:if="{{channel === 'LOCAL'}}" 包住。
4. node --check pages/address/list.js

### 测试断言
- check 零输出
- 同城确认页进列表带标签；无坐标地址「需补充定位」进编辑
- 邮寄确认页进同一入口 → 列表与改动前逐字相同（必须实点）
- Network：列表无 /local/quote 请求

### Done
- [ ] LOCAL 三态标签 + EXPRESS 零回归
- [ ] 无逐地址报价；注释到位
- [ ] 中文 commit：feat(miniapp): 地址列表同城选择模式（可配送状态 + 补充定位入口）

### 明确不做
- 不改 address/edit；不做道路距离/运费行；不改 confirm.js
- 不 push/密钥/生产/:3100

完成后用简短中文回报：三态验证 + 邮寄回归 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 4 地址列表同城选择模式 |
| 文件清单 | pages/address/list.{js,wxml,wxss} |
| 接口契约 | /addresses；/local/meta；禁逐行 quote |
| 测试断言 | 三态标签；邮寄零回归；无 quote 请求 |
| Done | 见上勾选 |
| 明确不做 | edit 页 / 道路距离 / 逐行报价 |
| 模型 | Terra |
