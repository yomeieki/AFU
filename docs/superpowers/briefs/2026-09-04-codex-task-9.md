# Codex Task 9 — 邮寄回归 + 预览台镜像 + 契约锁 + 文档

**模型档：Terra**（含回归判断与契约锁；勿升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 9（约 L946）
**拍板 D-007：** D-007 已拍板：①首页临时入口方案 A（同城/邮寄双卡片）②地址列表只显「直线约 x.x km」不调道路距离③同城不做立即购买，只加购→结算。（回归时核对：首页双卡不影响邮寄；地址列表邮寄无道路距离标签；无同城立即购买残渣）

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 9，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100（契约锁连已有后端）。

工作目录：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

硬约束（全部写死，不得违反）：
- 工作目录必须 cd 到活线后再动手。
- 后端 :3100 / 库 food_shop_sc 常驻：勿另起、勿杀。
- 不 push、不碰密钥、不改生产、不做真机真钱。
- 邮寄路径零回归；不改 pages/order/confirm.js 的 deliveryType EXPRESS 写死。
- 禁止扩 scope：不做封面页、不做 KD100/飞鹅、不动 apps/admin、不加固 M1/M2、不做预约/搜索/会员。
- ES5 风格；金额/距离一律来自服务端。
- 提交用中文 type(scope): 摘要 + Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>。

目标：逐条邮寄回归（不许只看代码）+ 预览台同城镜像 + 第 34 段顾客端字段契约锁 + spec 补丁。

### 文件清单
- Modify: tools/miniapp-preview/serve.mjs、tools/miniapp-preview/index.html
- Create: tools/miniapp-preview/pages/local-index.html、local-confirm.html
- Modify: tools/miniapp-preview/pages/order-detail.html（补同城态）
- Modify: scripts/e2e.sh（第 34 段）
- Modify: docs/superpowers/specs/2026-09-03-local-delivery-design.md
- （若 Task 6 未改完）docs/miniapp-release-checklist.md 可一并核对

### 接口契约（只锁字段名，不新建资源）
- GET /api/local/meta 公开 key 集
- POST /api/local/quote key 集（复用计划中的 LADDR 变量）
- GET /api/orders/<LOCAL_ORDER_ID> 详情 + delivery 白名单/敏感字段排除
- GET /api/orders 列表 deliveryType

### 步骤要点
1. 邮寄回归 10 条（开发者工具逐条点）：首页布局；分类加购 toast「已加入购物车」非同城+角标；有货购物车无跨渠道提示；空态跨渠道；邮寄 confirm deliveryType 仍 EXPRESS（禁止改写死）；地址列表邮寄无标签无 meta；邮寄详情快递卡+无 /courier；列表「邮寄」；邮寄详情两按钮；商家端定位仍可用。
2. 预览台：PAGE_WXSS/PAGE_COMPONENTS 加 local-index（sku-popup,empty-state）；画廊 iframe；baked mock 正常态；order-detail 同城态副本。跑 preview:miniapp 目检无样式冲突。
3. scripts/e2e.sh 第 34 段按计划粘贴（插在 33 段后、清理段前）；不新建资源。执行该脚本全绿；隔 60s 再跑一轮幂等。
4. spec 补丁：§2.1 Task0 结论；§3.1.3 改为直线粗估口径+理由；§6 页面清单 M3 已落地+临时入口说明；§9 链到本计划；§10 验证表更新。

### 测试断言
- 回归表 10/10 通过
- 契约锁两轮全绿，总数=原有+本段新增
- 预览台新页可见且不裂样式
- pages/order/confirm.js 仍含 deliveryType EXPRESS

### Done 勾选
- [ ] 邮寄回归清单勾完
- [ ] 预览台镜像 + 第34段契约锁 + spec 补丁
- [ ] 中文 commit（计划 Step 5）

### 明确不做
- 不做真机真钱 / 封面 / KD100 / 飞鹅 / 文档大翻修（order-flow/staff-guide/api 同城章属 M4）
- 不杀 :3100；不 push

完成后回报：回归 10 条结果表 + 契约锁两轮摘要 + 文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 9 邮寄回归+预览台+契约锁+文档 |
| 文件清单 | miniapp-preview/*；scripts/e2e.sh；specs 设计文档 |
| 接口契约 | 锁 meta/quote/orderDetail/delivery/list.deliveryType |
| 测试断言 | 回归 10 条；契约锁双轮；EXPRESS 写死仍在 |
| Done | 见上勾选 |
| 明确不做 | 真钱联调 / 封面 / M4 文档 / push |
| 模型 | Terra |
