# Codex Task 1 — 请求层透出错误码 + API 层渠道化 + 首页临时双入口

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 1（约 L177）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 1，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken（本 Task 只铺 API，不下单）。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

D-007 知悉：首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

目标：请求层透出业务码 + 同城 API 层 + 首页临时双入口（方案 A）。不实现 pages/local/*（Task 2/5）。

### 文件清单
- Modify: apps/miniapp/utils/request.js
- Create: apps/miniapp/api/local.js
- Modify: apps/miniapp/api/cart.js、apps/miniapp/api/order.js
- Modify: apps/miniapp/app.js、apps/miniapp/app.json
- Modify: apps/miniapp/pages/index/index.{js,wxml,wxss}

### 接口
- Consumes（已由 M1/M2 落地，本 Task 不改服务端）：GET /local/meta、POST /local/quote、GET /cart?channel=、POST /orders/:id/cancel-request、GET /orders/:id/courier
- Produces：api/local.js 的 getLocalMeta/quoteLocal；request() 的 err.code / err.data；silent 选项

### 步骤要点
1. utils/request.js：doRequest 加 silent=false；两处 reject 前挂 err.code（body.code 数字，否则 null）与 err.data；fail 分支同样 if (!silent) toast 且 err.code=null；自动重登录重试透传 silent。
2. api/local.js：getLocalMeta 用 silent:true（首页不能因同城挂了弹 toast）；quoteLocal(addressId, subtotal) 用 silent:true POST /local/quote。
3. api/cart.js：getCart(channel) 省略=EXPRESS；LOCAL 时 '?channel=LOCAL'。api/order.js 追加 requestCancelOrder、getCourierLocation（均 silent:true）并导出。
4. app.js updateCartCount：getCart() 改 getCart('EXPRESS')，注释说明 tabBar 角标只统计邮寄。
5. app.json pages 末尾注册 pages/local/index、pages/local/confirm（隐私相关留给 Task 6；requiredPrivateInfos 保持只有 chooseLocation）。
6. 首页临时双入口 A：index.js data.localEntry；loadLocalEntry 单独调 getLocalMeta（勿塞进 Promise.all）；副标题按 enabled/paused/isOpen/正常四态；goLocal 仅 clickable 时 navigateTo('/pages/local/index')（封面入口契约逐字不变）；onLoad/onPullDownRefresh 调 loadLocalEntry。wxml 在 banner 后、Categories 前插双卡片（同城+全国邮寄）；wxss 补 channel-entry 样式。
7. 静态校验：node --check 各改动 js + JSON.parse app.json，期望无输出。

### 测试断言
- 上述 check 命令零输出
- 开发者工具首页顶部两张卡片；同城副标题随后台开关/暂停/营业时段变化
- 点同城跳到尚不存在的 pages/local/index 报「页面不存在」——预期（Task 2 补）
- 邮寄首页其余部分与 tabBar 角标行为不变

### Done
- [ ] request 透出 code/data + silent；local/cart/order API 就绪
- [ ] 首页双入口可见且 goLocal 契约正确
- [ ] check 通过；未改服务端、未改 confirm.js EXPRESS、未启停 :3100
- [ ] 中文 commit：feat(miniapp): 请求层透出业务码 + 同城 API 层 + 首页同城临时入口（Co-Authored-By 按计划）

### 明确不做
- 不实现 pages/local/*、不做隐私弹窗、不做封面落地
- 不改 pages/order/confirm.js；不做同城立即购买
- 不 push、不密钥、不生产、不扩 M1/M2、不做 KD100/飞鹅

完成后用简短中文回报：做了什么 + 改动文件列表 + check 结果。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 1 请求层码透出 + API 渠道化 + 首页双入口 A |
| 文件清单 | request.js；api/local.js；api/cart.js；api/order.js；app.js；app.json；pages/index/* |
| 接口契约 | GET /local/meta；POST /local/quote；GET /cart?channel=；cancel-request；courier；err.code |
| 测试断言 | node --check 零输出；双卡片；点同城页不存在预期；邮寄角标不变 |
| Done | 见上勾选 |
| 明确不做 | local 页 / 隐私 / 封面 / confirm EXPRESS / 密钥 push :3100 |
| 模型 | Terra |
