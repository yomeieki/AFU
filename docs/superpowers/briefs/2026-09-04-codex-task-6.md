# Codex Task 6 — 官方隐私授权弹窗

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 6（约 L730）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 6，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken（本 Task 不涉及报价）。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

目标：官方隐私授权弹窗（__usePrivacyCheck__ + onNeedPrivacyAuthorization）。requiredPrivateInfos 保持只有 chooseLocation，不加 getLocation。

### 文件清单
- Create: apps/miniapp/components/privacy-popup/index.{js,json,wxml,wxss}
- Modify: apps/miniapp/app.js、apps/miniapp/app.json
- Modify: apps/miniapp/pages/address/edit（json+wxml 挂载）、apps/miniapp/pages/merchant/index（json+wxml 挂载）
- Modify: docs/miniapp-release-checklist.md

### 接口
- 微信：wx.onNeedPrivacyAuthorization、button open-type=agreePrivacyAuthorization、wx.openPrivacyContract

### 步骤要点
1. app.json 顶层 "__usePrivacyCheck__": true。
2. privacy-popup 半屏：标题「隐私保护指引」+ 正文（仅主动选点时取位置）+「拒绝」/「同意并继续」。「同意」必须 button open-type="agreePrivacyAuthorization" bindagreeprivacyauthorization——普通 bindtap 无效。指引可点→openPrivacyContract，失败回退 /pages/legal/index?type=privacy。拒绝只关弹层，无降级恐吓文案。
3. app.js onLaunch：若 onNeedPrivacyAuthorization，把 resolve 存 globalData.privacyResolve，找当前页 #privacy-popup show()；无组件则 resolve disagree（防卡死）。
4. 挂载页：仅 address/edit、merchant/index（会调 chooseLocation）。json 注册组件，wxml 末尾 <privacy-popup id="privacy-popup" />。
5. checklist：79-80 ⚠️→✅ 已启用说明；上架自查加两条（首次进编辑弹、拒绝后再点再弹）；页面清单加 pages/local/index|confirm。
6. JSON.parse app.json + node --check app.js + privacy-popup/index.js。开发者工具：清缓存→地址编辑→地图选点→弹窗→拒绝不报错→再点再弹→同意开地图。

### 测试断言
- check 零输出；四步链路通
- checklist 无「需重新评估」残留

### Done
- [ ] 组件+监听+两页挂载+文档同步
- [ ] 中文 commit：feat(miniapp): 官方隐私授权弹窗（__usePrivacyCheck__ + onNeedPrivacyAuthorization）

### 明确不做
- 不加 getLocation；不改 confirm.js；不做业务下单
- 不 push/密钥/生产/:3100

完成后用简短中文回报：链路验证 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 6 官方隐私授权弹窗 |
| 文件清单 | components/privacy-popup；app.js/json；address/edit；merchant/index；checklist |
| 接口契约 | onNeedPrivacyAuthorization；agreePrivacyAuthorization |
| 测试断言 | 拒绝/再弹/同意开地图；checklist 无残留警告 |
| Done | 见上勾选 |
| 明确不做 | getLocation / 业务下单 / 密钥 |
| 模型 | Terra |
