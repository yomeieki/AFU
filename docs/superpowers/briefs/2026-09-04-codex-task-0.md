# Codex Task 0 — `<map>` 能力前置检查

**模型档：Terra**（机械探测+写笔记；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 0
**拍板 D-007（本任务不涉及 UI 入口，仅知悉）：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 0，不扩 scope、不写业务页面、不碰生产/密钥/push、不另起/杀掉 :3100。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

目标：完成 `<map>` 能力前置检查，决定 Task 7 走 `<map>` 还是 cover-view 静态方案。本任务不阻塞 Task 1–6。

### 文件清单
- Create: docs/superpowers/notes/2026-09-04-map-probe.md
- Modify: docs/superpowers/specs/2026-09-03-local-delivery-design.md（仅 §2.1 末尾追加一行实测结论）
- 临时探针页：可在开发者工具里临时新建，**不要提交进仓库**

### 步骤
1. 在微信开发者工具临时页只放（无腾讯位置服务 key）：
   <map style="width:100%;height:400px" latitude="29.339" longitude="104.778"
        markers="{{[{id:1,latitude:29.339,longitude:104.778,width:24,height:24}]}}" />
   观察：能否渲染、Console 有无 key 相关报错、水印/授权提示。
2. 真机预览同一页（模拟器与真机都要看）。
3. 写笔记 docs/superpowers/notes/2026-09-04-map-probe.md，四行：
   - 模拟器：…
   - 真机：…
   - 结论：可用 / 有 key 限制 / 完全不可用
   - Task 7 走哪条路：`<map>` 或 cover-view 静态（门店/顾客两点+文字距离+「骑手距您约 x.x km」；轮询/生命周期/异常文案仍照做）
4. 在 specs/2026-09-03-local-delivery-design.md 的 §2.1 末尾追加一行实测结论（指向上述笔记）。

### 测试断言
- 笔记四行齐全，结论三选一明确
- spec §2.1 有对应一行，无其他无关改动
- git status 不含临时探针页

### Done 定义
- [ ] map-probe.md 存在且含「模拟器 / 真机 / 结论 / Task 7 路线」
- [ ] spec §2.1 末尾有实测结论一行
- [ ] 未提交临时探针页；未改 apps/miniapp 业务代码
- [ ] 中文 commit：`docs(m3): 记录 map 能力前置检查结论`（若需 commit；Co-Authored-By 按计划要求）

### 明确不做
- 不实现 pages/local、不改 confirm、不做 Task 1–9
- 不申请 lbs.qq.com key（仅记录是否需要留给 M4）
- 不阻塞后续任务开工

完成后用简短中文回报：四行结论摘要 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 0 map 前置检查 |
| 文件清单 | notes/map-probe.md；specs §2.1；临时页不入库 |
| 接口契约 | 无 |
| 测试断言 | 四行结论齐全；spec 一行；无临时页入库 |
| Done | 见上勾选 |
| 明确不做 | 业务码 / Task1–9 / 申请 key |
| 模型 | Terra |
