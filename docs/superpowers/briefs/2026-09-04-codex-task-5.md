# Codex Task 5 — `pages/local/confirm` 报价、下单与全部失败路径

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 5（约 L528）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 5（本计划核心），不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 下单必须带 quoteToken；payAmount = subtotal + quote.fee，quote 为 null 时显示「运费计算中」从不显示 ¥0.00 运费。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

动手前先读计划 §报价凭证生命周期 与 §失败与金钱路径速查表。

目标：创建 pages/local/confirm——唯一报价入口 refreshQuote、五触发点、提交与错误分流。

### 文件清单
- Create: apps/miniapp/pages/local/confirm.{js,wxml,wxss,json}
- Modify: apps/miniapp/api/order.js（createOrder 可选 silent；仅同城确认页传 true）

### 接口
- Consumes：GET /cart?channel=LOCAL、GET /addresses、GET /local/meta、POST /local/quote、GET /orders/meta、POST /orders
- Produces：wx.redirectTo('/pages/order/detail?id=<id>&autopay=1')
- createOrder body：deliveryType:'LOCAL' + quoteToken（必填）+ cartItemIds + addressId + remark

### 步骤要点
1. data：cartItemIds/items/subtotal/address/meta/headNotice/headBlocking；quoting/quote/quoteToken/quotedAt/quoteError/blockReason；needTableware/remark/payAmount/submitting；subscribeTemplateIds/payTimeoutMin。
2. 唯一报价入口 refreshQuote(reason)：无地址/无 latE6 → blockReason + quoteToken=null；quoting=true；quoteLocal(addressId, subtotal)。成功按 enabled/paused/!isOpen/!inRange/belowMin 设 blockReason 或 quoteToken=q.quoteToken；payAmount=subtotal+(q.fee||0)。catch：不清旧展示；42901 文案+3s 自动 retry 一次。quoteToken 为 null 时提交一律禁用。
3. 五触发点：onLoad 后 refreshQuote('load')+_quotedOnce；onShow 取 selectedAddress→'address'，否则 _quotedOnce 后 'show'（挡 onLoad 后立即 onShow 重复）；步进/删除 debounce 500ms→'subtotal'；提交前 quotedAt>10min→'stale' toast 不提交；42227/42239→见下。onUnload clearTimeout。
4. 页面：黄条（用 quote 的 enabled/paused/isOpen）→地址卡（缺定位红字+去补充；跳 list?mode=select&channel=LOCAL）→配送信息条（distanceM 非 straight；预计 now+estimatedMinutes；quoting 骨架不显旧数；quoteError+重试）→商品步进删除→备注/餐具（[需要餐具] 前缀 slice 255）→协议行→底条按 quoting/quoteError/blockReason/quoteToken 四态文案。超范围等时上方「换个地址」「改用全国邮寄」。
5. onSubmit：订阅授权在手势内→doSubmit。createOrder silent。handleSubmitError：42239/42227 toast+feeFlash+refreshQuote 绝不自动重提交；42223 modal 去补充定位；42220/42222/42226 blockReason+toast；42210/42230/42224 modal；其余 toast。
6. node --check pages/local/confirm.js

### 测试断言（对照速查表）
1 正常下单→详情 2 改门店坐标→42227闪+重报 3 改地址坐标→42239 4 数量掉免运→500ms运费变 5 超范围红条+出路 6 打烊禁用 7 暂停+原因 8 清坐标→去补充 9 连点数量 debounce 不 42901
- payAmount 始终=小计+quote.fee；从不 ¥0.00 运费；详情 shippingFee 与提交前一致

### Done
- [ ] 九条手工验收通过；quoteToken 缺则不可提交
- [ ] 未改 pages/order/confirm.js EXPRESS
- [ ] 中文 commit：feat(miniapp): 同城确认页——报价凭证生命周期与失败路径分流

### 明确不做
- 不做同城立即购买；不改邮寄确认页行为（仅 order API 可选 silent）
- 不 push/密钥/生产/:3100；不扩服务端

完成后用简短中文回报：九条验收勾选摘要 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 5 同城确认页报价凭证与失败分流 |
| 文件清单 | pages/local/confirm.*；api/order.js（silent） |
| 接口契约 | /local/quote + quoteToken；POST /orders deliveryType LOCAL |
| 测试断言 | 九条手工；payAmount=小计+fee；无 ¥0 运费 |
| Done | 见上勾选 |
| 明确不做 | 立即购买 / 改 EXPRESS confirm / 扩服务端 |
| 模型 | Terra |
