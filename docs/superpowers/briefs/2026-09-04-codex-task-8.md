# Codex Task 8 — 订单列表渠道标签

**模型档：Luna**（纯 UI 标签+一词分叉，机械改文件；若需大重构升 Terra）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 8（约 L912）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Luna。只做 M3 Task 8，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken（本 Task 不涉及报价）。服务端零改动。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

目标：订单列表渠道小标签 + SHIPPED 文案按渠道分叉。不做列表渠道筛选 Tab。

### 文件清单
- Modify: apps/miniapp/pages/order/list.{js,wxml,wxss}

### 接口
- Consumes：GET /orders（含 deliveryType），无需服务端改动

### 步骤要点
1. 订单号左侧标签：LOCAL→「同城」色 #e5441e soft；否则「邮寄」#3b5bdb soft（与工作台渠道色一致）。
2. STATUS_LABEL 按 deliveryType 选表：SHIPPED 邮寄「已发货」、同城「配送中」（仅差这一词）。
3. 不做渠道筛选 Tab。
4. node --check pages/order/list.js

### 测试断言
- 同城单标「同城」且 SHIPPED=「配送中」；邮寄单标「邮寄」且「已发货」
- 点进各自详情分支正确

### Done
- [ ] 标签+文案分叉；无筛选 Tab
- [ ] 中文 commit：feat(miniapp): 订单列表渠道标签与同城状态文案

### 明确不做
- 列表筛选 Tab；不改服务端；不改 confirm.js
- 不 push/密钥/生产/:3100

完成后用简短中文回报：列表验收摘要 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 8 订单列表渠道标签 |
| 文件清单 | pages/order/list.{js,wxml,wxss} |
| 接口契约 | GET /orders.deliveryType |
| 测试断言 | 同城/邮寄标签色与 SHIPPED 文案 |
| Done | 见上勾选 |
| 明确不做 | 筛选 Tab / 服务端 |
| 模型 | Luna |
