# Codex Task 2 — `pages/local/index` 同城菜单页

**模型档：Terra**（写死；不要升 Sol）
**活线：** `/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20`
**计划：** `docs/superpowers/plans/2026-09-04-local-delivery-m3-customer.md` → Task 2（约 L333）
**拍板 D-007：** 首页双入口 A；地址列表只显直线粗估；不做同城立即购买。

---

## 可贴 prompt（整段复制给 Codex）

```
你是 Codex，模型档 Terra。只做 M3 Task 2，不扩 scope、不碰生产/密钥/push、不另起/杀掉 :3100、不扩 M1/M2、不做封面/KD100/飞鹅。
不改 pages/order/confirm.js 的 deliveryType:'EXPRESS' 写死。金额/距离只来自服务端 quote；LOCAL 必须 quoteToken（本 Task 不做确认页报价）。

工作目录（必须 cd 到这里再动手）：
/Users/yumingyi/food-shop/.claude/worktrees/same-city-delivery-plan-2ffa20

D-007：首页双入口 A；地址列表只显直线粗估；不做同城立即购买（sku-popup mode 固定 'cart'）。

目标：创建 pages/local/index 同城菜单页（店头状态 + 左分类右商品 + 独立 LOCAL 购物车条）。确认页留给 Task 5。

### 文件清单
- Create: apps/miniapp/pages/local/index.{js,wxml,wxss,json}

### 接口
- Consumes：GET /local/meta、GET /categories?channel=LOCAL、GET /products?channel=LOCAL&…、GET /cart?channel=LOCAL、POST /cart（及既有 update/delete cart）
- Produces：wx.navigateTo('/pages/local/confirm?cartItemIds=1,2,3')

### 步骤要点
1. index.json：navigationBarTitleText「同城配送」；usingComponents sku-popup、empty-state。
2. data：meta/headNotice/headBlocking/categories/activeCategoryId/list/page/hasMore/loading/cartItems/cartCount/cartAmount/cartExpanded/skuShow/skuProduct。
3. 页头：门店名+营业胶囊 → 范围/起送/基础运费 → 黄条。headNoticeOf(meta) 三态：enabled=false「即将开通」+「去全国邮寄」且整页不渲染商品；paused「暂停接单：reason」可挑不可结算；!isOpen 用 nextOpenText，同上。
4. 左分类右商品：复用 product/list 两栏思路，复制必要 wxss（勿跨页 import）。请求一律 channel=LOCAL。分类空 → empty-state「同城菜单还在准备中」。
5. 加购：sku-popup mode='cart'；toast「已加入同城购物车」；loadCart()，不调 app.updateCartCount()。底条：共 N 件·¥xx +「去结算」；未达起送显示「还差 ¥x 起送」禁用（唯一允许的前端金额比较：minOrderAmount vs 小计）。点条展开明细（步进/删除）。去结算：全部 LOCAL 行 id 拼 query（无勾选态）。
6. onShow 重拉 meta+cart；商品列表仅 onLoad/切分类拉。
7. node --check apps/miniapp/pages/local/index.js

### 测试断言
- check 零输出
- 首页→同城：可浏览/加购/底条件数正确
- 后台暂停→返回：黄条+结算禁用；关总开关：仅「即将开通」一屏
- 同城加购后 tabBar 购物车角标不变

### Done
- [ ] 四态（正常/暂停/打烊/未开通）可演示
- [ ] 角标隔离证据成立；无立即购买入口
- [ ] 中文 commit：feat(miniapp): 同城菜单页（店头状态 + 分类商品 + 独立购物车条）

### 明确不做
- 不做 pages/local/confirm、不做立即购买、不做地址/隐私/详情
- 不改 confirm.js EXPRESS；不 push/密钥/生产/:3100

完成后用简短中文回报：四态验证摘要 + 改动文件列表。
```

---

## 拆任务字段（本条）
| 字段 | 内容 |
|---|---|
| 任务名 | Task 2 同城菜单页 pages/local/index |
| 文件清单 | pages/local/index.{js,wxml,wxss,json} |
| 接口契约 | /local/meta；/categories|/products|/cart channel=LOCAL；navigateTo confirm?cartItemIds= |
| 测试断言 | check；四态；角标不变；无立即购买 |
| Done | 见上勾选 |
| 明确不做 | confirm 页 / 立即购买 / 密钥 push :3100 |
| 模型 | Terra |
