# 预约送达真机走查后五个 S 级小修 · Opus 复核记录（2026-09-22）

范围：main a88e660..3b54169（75d0c01 小程序时段选择器入口、2099c63 折叠组已接单卡按钮、5234612 四处辨识、10dd6bd 图例补徽章、3b54169 图例缩短）。
复核者：Opus，新会话，只给需求简报 + diff + 验证数字。裁决：Fable。收口执行：Sonnet，提交 2211203。

## 第一轮结论：存在阻断项（R1 需改）

- **R1 需改 → 已修**：`pages/local/confirm.wxml` 「更改 ›」是独立 `wx:if`，与前面 if/elif 链不互斥；已选时段 → 42291 重拉 → 重拉失败时与红色错误文案同时渲染，且 `openPicker` 以 `hasAnySlot` 为前提、此时为 false，点了没反应。复核者用探针脚本复现。修法：条件改 `slotSelected && hasAnySlot && !slotsError`。
- **R2 建议 → 已修**：`scheduleCapsule` WAITING 前缀「待接单/已接单」在非待接单列也会出现（折叠组卡用「立即呼叫」提前发单后落到等待配送员列，phase 仍 WAITING）。修法：前缀限 `colKey === 'pending'`，新增测试。
- **R3 建议 → 已修**：抽屉头胶囊未传 `card.status`，与卡片文案不一致。
- **R4 规划缺口 → 不改（店主口径）**：折叠组全是已接单预约单且待接单列为空时，手机页签显示「待接单 0」但点进去仍有卡；桌面有折叠组头兜底。
- **R5 建议 → 不改（待店主定色）**：`wb__badge--sched` 复用「该催了」的琥珀色变量；浅色主题对比度 2.56:1（同城红 3.64、深色 7.91）。
- **R6 建议 → 已修**：注释残留旧图例文案。

第一轮已核实：五处改动各自满足需求、未越出授权范围；折叠组已接单卡走「备餐中」按钮分支时 `active === null` 成立（服务端 `workbench.ts:197` 只把无配送单的单放进 scheduled 桶），`/ready` 只校验 PREPARING、force 呼叫走 MANUAL_EARLY，两个键都真打得到服务端；新增测试为行为断言；三套测试数字与独立复跑一致；ES5 检查通过。

## 再复核结论：无阻断项

- R1/R2/R3/R6 逐条确认已解决。R1 枚举五态：打烊自动选中、原格消失仍有别格、42291 重拉成功、重拉失败、重拉成功但一格不剩；入口出现 ⟺ `openPicker` 真会开弹层。
- **R7 建议（新，既有问题，未处理）**：时段全部消失（hasAnySlot=false、无错误）时，行内仍写「该时段已过，请重选」，但已无任何可重选去处。建议后续改成「该时段已过，暂无可选时段」。
- fix diff 与 worktree `git diff` 逐字节一致；4 个文件均在授权范围并集内。

## 验证数字（复核者独立复跑 = 执行者上报）

小程序 308/0；后台 utils 93/0（含 npm test 110/0）；tsc 0 错；ES5 ✔；build:admin ✓。

## 无法核实（留待真机）

stale 态三段文字并排（删除线 + 「请重选」 + 「更改 ›」）在 flex 行里的观感；图例在平板宽度的折行（本机量宽：≥1248 一行，1024 分两组两行）。
