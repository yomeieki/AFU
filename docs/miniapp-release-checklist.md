# 小程序上架提审清单

> 写给店铺运营者。按顺序逐项打勾,全部完成后提审。代码侧(第四节)由开发完成,其余在微信公众平台 / 商户平台 / 腾讯云控制台操作。
> 小程序 AppID:`wx11aa80fcd1bfd2bb`　名称:**阿福凉菜**
>
> **状态(2026-09-02):平台侧配置与服务端全部就绪,仅剩「真机一分钱支付+退款实测」未做。**

---

## 一、主体与资质准备(提审前必须备齐)

| 材料 | 用途 | 状态 |
|---|---|---|
| 营业执照 | 自贡市高新区阿福凉菜丹桂店 / 92510300MA634FDC4K | ✅ |
| 食品经营许可证 | CSJ01303000130051 | ✅ |
| 法人/管理员微信 | 公众平台登录、提审 | ✅ |
| 客服电话 | 15309003232(小程序「联系商家」直拨) | ✅ |
| 门店地址、营业时间 | 四川省自贡市汇东新区丹桂40栋底楼 / 9:00-20:00 | ✅ |

**以上信息已全部填入 `apps/miniapp/config/shop.js`(2026-09-02),协议与隐私政策全文自动引用,已无「【待填】」残留。** 日后店铺信息变更只改这一个文件:

| 文件 | 字段 | 填什么 |
|---|---|---|
| `apps/miniapp/config/shop.js` | `companyName` | 营业执照主体全称 |
| | `phone` | 客服电话,纯数字 |
| | `address` / `businessHours` | 门店地址 / 营业时间 |
| | `licenseNo` | 统一社会信用代码 |
| | `foodLicenseNo` | 食品经营许可证编号 |
| | `contactEmail` | 联系邮箱(隐私政策要求) |
| `apps/miniapp/config/legal.js` | 自动引用 shop.js | 无需单独改;如需调整条款文案在此改 |

---

## 二、微信公众平台基础设置(mp.weixin.qq.com)

### 2.1 基本信息(设置 → 基本设置)
- [x] 小程序名称:**阿福凉菜**(名称含「凉菜」属食品类,需与类目一致;不用「商城」二字避免电商平台类目审核)
- [x] 头像:印章 logo(方形,≥144×144)
- [x] 简介:例「丹桂阿福凉菜,三十年老店。同城配送 / 全国邮寄,凉菜熟食在线下单」(4-120 字,不含联系方式)
- [x] 小程序备案:**已备案**(在 AppID `wx11aa80fcd1bfd2bb` 上)

### 2.2 服务类目(设置 → 基本设置 → 服务类目)
- [x] 类目已选定并**上传食品经营许可证**(2026-09-02 完成;提审前核对证件未过期)

### 2.3 服务器域名(开发 → 开发管理 → 开发设置 → 服务器域名)
| 类型 | 域名 | 状态 |
|---|---|---|
| request 合法域名 | `https://api.yuegui-hotel.online` | ✅ |
| uploadFile 合法域名 | `https://api.yuegui-hotel.online` | ✅ |
| downloadFile 合法域名 | `https://afu-images-1342627167.cos.ap-shanghai.myqcloud.com` | ✅ |
| **业务域名** | `https://admin.yuegui-hotel.online`(小程序「商家管理」内嵌后台 web-view 用) | ☐ **需要**:下载校验文件 `MP_verify_xxx.txt` 放到 `apps/admin/public/`,重新部署后台后再在公众平台点校验 |

> 每月只能修改 5 次,一次配齐。图片桶 `afu-images-1342627167`(上海 ap-shanghai,公有读私有写)已于 2026-09-02 建好。

### 2.4 用户隐私保护指引(设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引)
**2026-09-03 修正并重新提交 ✅。**

> ⚠️ 此前记录为「2026-09-02 已填写完成」，但 2026-09-03 复核发现实际保存的是第一档
> 「本小程序开发者承诺并保证，**未以任何方式处理用户的任何信息**」——而小程序一直在收集
> 收货人姓名、手机号、收货地址与剪贴板。下表的勾选项只在选择第二档后才存在，故当时并未真正生效。
> 现已改为第二档并如实勾选。**日后核对此项时，先确认选的是第二档，再看勾选清单。**

当前勾选（公众平台选项名以 2026-09 版为准）:

| 信息类型 | 是否勾选 | 用途填写 |
|---|---|---|
| **收货地址** | ✅ 勾 | 用于配送与邮寄商品 |
| **手机号** | ✅ 勾 | 用于配送联系(**用户在收货地址中手动填写,非微信手机号授权**) |
| 昵称、头像(用户信息) | 视情况:当前代码**不调用** `getUserProfile`,登录只取 OpenID → **可不勾**;如后续接入再补 | — |
| **剪贴板** | ✅ 勾 | 「关于我们」页点击地址复制到剪贴板 |
| **选择的位置信息** | ✅ 勾 | 同城配送在地图选点（`wx.chooseLocation`，2026-09-03 已开通接口权限）。商家端「设置门店位置」（`pages/merchant/index.js` `onSetStoreLocation`）复用同一接口，同样是地图选点、非自动定位 |
| 位置信息 | ❌ 不勾 | 对应 `wx.getLocation` 自动定位，未申请、未使用 |
| 相册/相机/麦克风 | ❌ 不勾 | — |
| 微信运动/蓝牙/日历 等 | ❌ 不勾 | — |

- 「开发者联系方式」填客服电话/邮箱
- 「隐私政策」处可填小程序内页面路径 `pages/legal/index?type=privacy`,或上传该页截图
- 保存后需**用户端弹窗确认**才生效(公众平台会提示),提审前确认状态为「已生效」
- ✅ 已启用 `__usePrivacyCheck__`，弹窗组件 `components/privacy-popup`，监听在 `app.js onLaunch`（M3 落地 2026-09-04）。
- 隐私政策全文（`config/legal.js`）已于 2026-09-03 补具名第三方共享条款：快递100 及其运力方、飞鹅云打印。
  微信的指引表单里没有「向第三方提供」的填写位，而其第 7.1 条要求共享须取得单独同意，故必须写在自有隐私政策中。

### 2.4b 隐私与位置能力一致性检查

2.4 节的内容分散在好几处(公众平台勾选项、代码里的隐私弹窗、`legal.js` 条款文案),容易出现"某一处改了、其余几处没跟着改"。上线前(以及日后任何改动位置相关代码之后)按下表一次性核对完:

| 检查项 | 怎么核对 | 当前状态(2026-09 核实) |
|---|---|---|
| `__usePrivacyCheck__` 是否开启 | `apps/miniapp/app.json` 顶层字段 | ✅ `true` |
| `requiredPrivateInfos` 是否**只有** `chooseLocation` | `apps/miniapp/app.json` | ✅ `["chooseLocation"]`——**不能悄悄多出** `getLocation`。一旦代码里加了 `wx.getLocation` 调用却没有同步在这里申请、也没有回 2.4 补勾"位置信息",小程序会在真机上直接报权限错误，而不是提审被打回——这个坑比审核打回更隐蔽 |
| 隐私弹窗 `buttonId` | `apps/miniapp/components/privacy-popup/index.wxml` 的同意按钮是否有 `id="privacy-agree-btn"`,`index.js` 的 `onAgree` 是否把 `buttonId` 一起传给 `resolve` | ✅ 已修复(M4 计划 F5,`55a20a2`),弹层被卸载/被新请求覆盖时的悬空 Promise 也已处理 |
| `config/legal.js` 的位置信息条款 | 隐私政策全文是否明确写了"同城配送需要在地图上选择收货位置/门店位置"这类条款,且与 2.4 表格里"选择的位置信息"一行的勾选状态一致 | ✅ 一致——都是"地图选点"语义(`wx.chooseLocation`),不涉及自动定位 |
| 实现方式是"选点"还是"自动定位" | 搜索代码里所有 `wx.chooseLocation`/`wx.getLocation` 调用点 | ✅ 全部是 `wx.chooseLocation`(顾客端地址编辑页、商家端"门店位置")；全仓库没有 `wx.getLocation` 调用，与 `requiredPrivateInfos` 只含 `chooseLocation` 互相印证 |

**改动检查提示**：任何改动 `pages/address/edit`、`pages/merchant/index`、`pages/local/confirm` 里位置接口调用的 PR，都要重新走一遍这张表——这是本节存在的意义，不是走一次流程就完事。尤其是"新加了 `wx.getLocation`"这种改动，**必须同步**改 `app.json` 的 `requiredPrivateInfos`、公众平台 2.4 的勾选、以及 `config/legal.js` 的条款文案三处，漏一处都会导致真机上位置功能报错或隐私合规不完整。

### 2.5 客服(功能 → 客服)
- 本小程序采用**拨打电话**方式,不需要配置微信客服人员;审核备注里写明「客服方式:小程序内「联系商家」直拨 xxx」即可

### 2.6 订阅消息(功能 → 订阅消息 → 公共模板库)
顾客付款时会弹一次授权,之后发货 / 退款到账会推微信通知。需要选 2 个**一次性**公共模板:
- [ ] 「发货通知」类(关键词建议:订单编号、快递公司、快递单号、发货时间)→ 记下模板 ID 与各字段的 key(如 `character_string1`)
- [ ] 「退款通知」类(关键词建议:订单编号、退款金额、退款原因、退款时间)
- [ ] 把模板 ID 和字段映射填入服务器 `.env` 的 `WECHAT_TMPL_SHIP / WECHAT_TMPL_SHIP_FIELDS / WECHAT_TMPL_REFUND / WECHAT_TMPL_REFUND_FIELDS`(格式见 `.env.example`),重启后「系统状态」页订阅消息两项变绿
- 未配置时小程序不会弹授权,其余功能不受影响

---

## 三、微信支付商户平台(pay.weixin.qq.com)

按 [payment-setup.md](payment-setup.md) 完成:
商户号 `1117182879`,**验签走「微信支付公钥」模式**。

- [x] APIv3 密钥已设置并填入 `.env`
- [x] API 证书已申请,`apiclient_key.pem` 在 `/www/food-shop/apps/server/keys/`(600),序列号 `393042462CFF8063CEF33A622E3D629081D37773`
- [x] 微信支付公钥 `pub_key.pem` 已上传,`WECHAT_PAY_PUBLIC_KEY_PATH` + `WECHAT_PAY_PUBLIC_KEY_ID` 已配(代码按回调头自动识别为 public-key 模式)
- [x] 商户号已关联小程序 AppID `wx11aa80fcd1bfd2bb`
- [x] 管理后台 → 系统状态:支付 7 项 + 回调验签 + COS 全绿
- [x] 商户凭证脱机自检通过:`node apps/server/scripts/check-wechat-pay.mjs`(调只读接口验穿商户号+序列号+私钥+APIv3 密钥,不花钱)
- [ ] **一分钱实测**:真机下单支付成功 → 后台变「待接单」→ 后台点「退款」两步确认 → 微信退款到账 → 订单变「已退款」
      ← **这是唯一还没验过的环节。回调验签只有等微信真的回调过来才知道公钥配对是否正确。**

> 审核员会真的走一遍下单流程。支付不通是食品/电商类小程序最常见的打回原因,**务必在支付跑通后再提审**。

---

## 四、代码与服务端自检(开发完成,提审前核对)

- [ ] `apps/miniapp/config/index.js`:`isDev = false`,`baseURL` = `https://api.yuegui-hotel.online/api`
- [ ] `apps/miniapp/project.config.json` 的 `appid` 与公众平台一致
- [ ] `config/shop.js` 无「【待填】」残留(`grep -rn 待填 apps/miniapp/config`)
- [ ] 「我的」→「关于我们」→「用户协议 / 隐私政策」可打开且排版正常
- [ ] 「我的」→「联系商家」能拉起拨号
- [ ] 下单页底部有「提交订单即表示同意《用户协议》和《隐私政策》」且可点击
- [ ] 首页 / 商品详情右上角「…」→ 转发,分享卡片标题带「阿福凉菜」、商品分享带封面图
- [ ] 服务器 `.env`:`WECHAT_*_MOCK` 全为 `false`,`COS_*` 已配置,`NODE_ENV=production`
- [ ] 服务器已跑 `migrate-uploads-to-cos.ts`(先 `--dry-run`),小程序里商品图片正常显示
- [ ] 管理后台 → 系统状态:全部分组无灰项(「可选」除外);「订单生命周期」显示 15 分钟 / 7 天 / 定时任务开启
- [ ] 服务器 `.env` 新增项:`PAY_TIMEOUT_MIN`、`AUTO_COMPLETE_DAYS`、`WECHAT_TMPL_*`(见 `.env.example`);部署后 `npx prisma migrate deploy`(新增 after_sales 表与订单字段)
- [ ] 真机:下单不付款,15 分钟后订单自动变「已取消」且商品库存恢复
- [ ] 真机:一分钱支付 → 后台部分退款 0.01 → 顾客订单页显示「已退款 -¥0.01」且订单状态不变 → 再全额退剩余 → 订单「已退款」
- [ ] 真机:已发货订单「申请售后」传 1 张图 → 后台「售后」标签同意退款 → 顾客端显示商家回复
- [ ] 真机:「我的 → 商家管理」登录后「进入管理后台」能在小程序内打开后台并已登录(需 2.3 业务域名生效)
- [ ] 真机:收货地址「导入微信收货地址」与省市区选择器可用
- [ ] 隐私弹窗在首次进入地址编辑页时出现
- [ ] 拒绝后再次点地图选点会再次弹出
- [ ] UptimeRobot 监控已建,企微告警群已收到「服务启动」测试消息
- [ ] 微信开发者工具:上传代码 → 公众平台「版本管理」出现开发版本 → 设为体验版 → 真机用体验版走完整流程

---

## 五、提审表单怎么填(版本管理 → 提交审核)

| 项 | 填写建议 |
|---|---|
| 版本描述 | 「阿福凉菜小程序 v1.0.0:商品浏览、购物车、微信支付下单、订单管理、收货地址、同城配送/全国邮寄」 |
| 功能页面 | 首页 `pages/index/index`(类目:餐饮/食品)、商品列表 `pages/product/list`、商品详情 `pages/product/detail`、同城配送 `pages/local/index`、同城结算 `pages/local/confirm` |
| 测试账号 | 选「**不需要**」:微信授权登录即可使用,无独立账号体系 |
| 审核备注 / 补充说明 | 见下方模板 |
| 是否采用「订单中心」 | 有实物交易,建议后续接入「小程序订单管理」(不影响本次审核) |

**补充说明模板**(直接粘贴,替换括号内容):
```
本小程序为「丹桂阿福凉菜」线下门店的线上点单工具,主体为(营业执照名称),持有食品经营许可证(编号)。
主要功能:浏览凉菜/熟食商品、加入购物车、微信支付下单、填写收货地址、查看订单与物流。
支付:已开通微信支付商户号(商户号 xxx),审核员可用任意商品下单体验,支付金额真实扣款后我们会全额退款。
退款:待接单前可自助申请退款;接单后请通过小程序内「联系商家」电话协商。
客服:小程序「我的 → 联系商家」直接拨打 (客服电话)。
用户协议与隐私政策:「我的 → 关于我们」内可查看;下单页底部有同意声明。
商家管理入口:「我的 → 商家管理」为店员内部使用,需要管理员账号登录,审核可忽略。
```

审核周期通常 1-7 天;通过后**手动点「发布」**才会上线。

---

## 六、常见打回原因与对策

| 打回理由 | 原因 | 对策 |
|---|---|---|
| 类目与内容不符 / 缺少资质 | 食品类目未传许可证或已过期 | 服务类目重新上传证件 |
| 未提供用户协议 / 隐私政策 | 审核员没找到入口 | 补充说明写明路径「我的 → 关于我们」 |
| 隐私保护指引未填写或与实际不符 | 指引未生效、勾选项与代码行为不一致 | 按 2.4 重填,确认「已生效」 |
| 支付功能不可用 | 商户号未配好 / 回调验签失败 | 系统状态页排查,一分钱实测通过后再提 |
| 页面存在无法打开的功能 | 图片 404(COS 域名未配 downloadFile)、接口报错 | 体验版真机全流程走一遍 |
| 未提供客服方式 | — | 补充说明写客服电话;「联系商家」入口截图 |
| 诱导分享 | 分享文案含「分享得奖励」等 | 当前文案无此问题,不要后续加 |
| 名称与类目不符 | 名称含「商城」但类目非电商平台 | 已改名「阿福凉菜」 |
| 商家管理入口需要账号 | 审核员点进「商家管理」被登录页拦住 | 补充说明注明「内部使用可忽略」 |

---

## 七、上线首日检查

- [ ] 公众平台「发布」后,微信搜索「阿福凉菜」能搜到并打开
- [ ] UptimeRobot 三个监控全绿
- [ ] 真实下单一笔(可自己买):企微群收到「新订单」推送 → 后台接单/发货流程 → 顾客端状态同步
- [ ] 后台退款一笔:两步确认 → 企微收到「退款已到账」
- [ ] `pm2 logs food-shop-server --err` 无 500;企微告警群无异常告警
- [ ] 备份 cron 手动跑一次 `bash /www/food-shop/scripts/backup.sh`,企微收到「备份完成」
- [ ] 随机打开 3 个商品,图片 URL 为 COS 域名且 200
- [ ] 管理后台手机端「添加到主屏幕」正常

---

## 八、回滚预案

| 场景 | 操作 |
|---|---|
| 小程序线上版本有严重问题 | 公众平台 → 版本管理 → 线上版本 → 「版本回退」(可回退到上一个已发布版本) |
| 后端部署出问题 | `DEPLOY_REF=<部署前 sha> bash scripts/deploy.sh`(每次部署结束脚本会打印这条带具体 sha 的命令,直接复制即可)。**不要**先手动 `git reset --hard <上一版>` 再裸跑 `bash scripts/deploy.sh`——脚本第 2 步会无条件 `git reset --hard` 到 `origin/main`(或 `DEPLOY_REF`),手动 reset 会被这一步原样覆盖掉,等于没回滚 |
| 数据库迁移出问题 | deploy.sh 在迁移前自动备份到 `/www/backups/pre-deploy/`,按打印的 `gunzip | mysql` 命令恢复。恢复流程本身已经实测演练过一次(见 `docs/ops-restore-drill.md`:18 张表逐项对齐、金额校验一致、全程未碰生产库),不是纸面流程 |
| 支付/退款回调异常 | 企微告警群会收到「金额不一致」「退款异常」;订单页可「手动标记完成」兜底,勿直接改数据库 |
