node_modules/
dist/
.env
*.env.local
# set-env.sh / import-secrets.sh 改 .env 前会留 .env.bak-<时间戳>，里面是完整密钥。
# 只写 `.env` 匹配不到它们，漏掉就可能被 `git add -A` 连密钥一起提交。
.env.bak-*
.DS_Store

# WeChat Pay private key and certificates (never commit)
*.pem
*.key
*.p12

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/

# Build output
apps/admin/dist/
apps/server/dist/

# Local uploaded images (migrate to COS later)
apps/server/uploads/

# WeChat DevTools
apps/miniapp/.idea/
apps/miniapp/node_modules/

# 小程序预览台：由源 wxss 实时派生，不入库
tools/miniapp-preview/*.generated.css

# 微信开发者工具个人配置
project.private.config.json

# Claude Code 本地设置
.claude/settings.local.json

# 验签自测生成的临时密钥
apps/server/.selftest/
