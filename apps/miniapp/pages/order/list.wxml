<view class="page">

  <!-- Tabs -->
  <scroll-view scroll-x class="tab-scroll" show-scrollbar="{{false}}">
    <view class="tab-list">
      <view
        wx:for="{{tabs}}"
        wx:key="index"
        class="tab-item {{activeTab === index ? 'active' : ''}}"
        bindtap="onTabChange"
        data-idx="{{index}}"
      >
        {{item.label}}
      </view>
    </view>
  </scroll-view>

  <view wx:if="{{loading}}" class="loading-wrap">
    <text class="tip-text">加载中...</text>
  </view>

  <view wx:elif="{{orders.length === 0}}" class="empty-wrap">
    <text class="empty-text">暂无订单</text>
  </view>

  <view wx:else class="order-list">
    <view
      wx:for="{{orders}}"
      wx:key="id"
      class="order-card"
      bindtap="goToDetail"
      data-id="{{item.id}}"
    >
      <view class="order-header">
        <text class="order-no">订单号：{{item.orderNo}}</text>
        <text class="order-status {{item.status}}">{{item.statusLabel}}</text>
      </view>

      <view class="order-item-row" wx:if="{{item.firstItem}}">
        <view class="order-item-img-wrap">
          <image
            wx:if="{{item.firstItem.productImage}}"
            class="order-item-img"
            src="{{item.firstItem.productImage}}"
            mode="aspectFill"
          />
          <view wx:else class="order-item-img-ph"><text>无图</text></view>
        </view>
        <view class="order-item-info">
          <text class="order-item-name">{{item.firstItem.productName}}</text>
          <text class="order-item-qty">× {{item.firstItem.quantity}}</text>
        </view>
        <text wx:if="{{item.moreCount > 0}}" class="more-count">等{{item.moreCount + 1}}件</text>
      </view>

      <view class="order-footer">
        <text class="order-amount">合计：¥{{item.actualAmountText}}</text>
        <view class="order-action">
          <text wx:if="{{item.status === 'PENDING_PAYMENT'}}" class="action-btn pay">去支付</text>
        </view>
      </view>
    </view>
  </view>

</view>
